apiVersion: v1
kind: Service
metadata:
  name: {{ include "payday-app.fullname" . }}
  labels:
    {{- include "payday-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payday-app.selectorLabels" . | nindent 4 }}
