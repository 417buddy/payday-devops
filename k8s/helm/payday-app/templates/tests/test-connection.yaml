apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payday-app.fullname" . }}-test-connection"
  labels:
    {{- include "payday-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payday-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
